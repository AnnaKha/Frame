using NUnit.Framework;
using OpenQA.Selenium;

namespace Core.WebElements.ComponentsSteps
{
	public class InputWithButtonsSteps
	{
		/*public void ValidateIncreaseDecreaseStep(int expectedStep)
		{
			int initialValue = int.Parse(GetValue);
			ClickIncrease();
			int increasedValue = int.Parse(GetValue);
			ClickDecrease();
			int decreasedValue = int.Parse(GetValue);
			Assert.That((increasedValue - initialValue).Equals(expectedStep));
			Assert.That((decreasedValue - increasedValue).Equals(expectedStep));
			Logger.LoggingHelper.LogInformation("Increase/Decrease step is correct");
		}*/
	}
}
